'use strict';
/**
 *
 */
angular.module('chibleeApp').controller('detailCtrl', function($scope, $http, api, $window, $cookieStore) {
    $scope.test = function() {
        alert('testing');
    };
    $scope.test();
});